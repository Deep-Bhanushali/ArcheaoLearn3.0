# Copy to .env and fill values
# Your Google Generative API key for the Generative Language API
GENERATIVE_API_KEY=
# Optional: the model id to use (check /list-models for available names)
GENERATIVE_MODEL=gemini-1.5-flash-latest

# Other env vars used by the project (examples)
MONGO_URI=
JWT_SECRET=your_jwt_secret
PORT=4242
FRONTEND_URL=http://localhost:5173
STRIPE_PRIVATE_KEY=

